/** --------------- Scroll Object Types --------------- */

/**
 * Holds the temp stored scroll position.
 * If apply is true, the scroll position is applied to the component.
 */
export declare type ScrollState = {
  pos: number,
  apply: boolean
}
