/** --------------- Router Object Types --------------- */

/** Holds the previous and current navigated url. */
export declare type RouteHistory = {
  currentRoute: string,
  previousRoute: string
}
